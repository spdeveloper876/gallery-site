backend:
  name: github
  repo: spdeveloper876/gallery-site
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "blog"
    label: "Blog"
    folder: "blog"
    create: true
    fields:
      - { name: "title", label: "Title", widget: "string" }